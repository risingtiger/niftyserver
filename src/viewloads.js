const load_home=(_pathparams,_searchparams)=>new Promise(async(res,_rej)=>{const d=new Map();res({d,refreshon:[]})});const load_adminsyncfrommastercsv=(_pathparams,_searchparams)=>new Promise(async(res,_rej)=>{const d=new Map();res({d,refreshon:[]})});const load_machines=(_pathparams,searchparams)=>new Promise(async(res,rej)=>{const d=new Map();const hasSearch=searchparams.search_field&&searchparams.search_val;const hasListView=searchparams.what&&searchparams.secondary;const promises=[];if(hasSearch){const field=encodeURIComponent(searchparams.search_field);const val=encodeURIComponent(searchparams.search_val);promises.push($N.FetchLassie(`/api/machines/search?field=${field}&val=${val}`,{method:"GET"}))}else if(hasListView){promises.push($N.FetchLassie(`/api/machines?what=${searchparams.what}&secondary=${searchparams.secondary}`,{method:"GET"}))}else{promises.push($N.FetchLassie('/api/machines?what=notok&secondary=none',{method:"GET"},{cacheit:'5s'}))}const results=await Promise.all(promises);if(!results[0].ok){rej();return}d.set("machines_view_list",results[0].data);res({d,refreshon:["machines"]})});const load_machinetelemetry=(pathparams,searchparams)=>new Promise(async(res,rej)=>{const d=new Map();const r=await $N.FetchLassie(`/api/machines/${pathparams.id}`);if(!r.ok){rej();return}const rd=r.data;d.set(`machines/${pathparams.id}/telemetry_view`,[rd]);res({d,refreshon:["machines/"+pathparams.id]})});const load_machine=(pathparams,searchparams)=>new Promise(async(res,rej)=>{const d=new Map();const promises=[];promises.push($N.FetchLassie(`/api/machines/${pathparams.id}/view?firestore_refid=${searchparams.firestore_refid}`,{method:"GET"}));const results=await Promise.all(promises);if(!results[0].ok){rej();return}d.set(`machines/${pathparams.id}/view`,results[0].data);res({d,refreshon:["machines/"+pathparams.id]})});const load_notifications=(_pathparams,_searchparams)=>new Promise(async(res,rej)=>{const d=new Map();const path='/api/notifications/get_users_schedules';const promises=[$N.FetchLassie(path,{method:"GET"})];const results=await Promise.all(promises);if(!results[0].ok){rej();return}d.set("users_schedules",results[0].data);res({d,refreshon:['users']})});export default{load_home,load_adminsyncfrommastercsv,load_machines,load_machinetelemetry,load_machine,load_notifications}